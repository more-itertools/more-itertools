sudo: false

language: "python"

python:
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"

install:
    - "pip install -U nose"
    - "pip install -e ."

script:
    - "mkdir -p build && cd build"
    - "nosetests more_itertools --with-doctest"

notifications:
  email: false
